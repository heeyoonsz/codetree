#include <iostream>
#include <string>
using namespace std;

int main() {
    // Please write your code here.
    string a,b;
    cin >> a >> b;
    
    // string part = a[0]+a[1];
    // string b_substr= b.erase(0,1);

    b[0] = a[0];
    b[1] = a[1];

    cout << b;

    return 0;
}